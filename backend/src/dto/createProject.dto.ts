import { ObjectEnumValue } from "../../generated/prisma/runtime/library.js";

export interface ProjectDto {
    name: string;
    summary: string;
    duration: string;
    category: string;
    priority: string;
}